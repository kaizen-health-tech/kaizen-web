**MemoryChip** — a piece of context Kai has stored ("Allergic to penicillin"). Lilac by default; tap to add and it fills violet with a check.

```jsx
<MemoryChip label="Allergic to penicillin" />
<MemoryChip label="Takes vitamin D" state="added" onToggle={toggle} />
```
