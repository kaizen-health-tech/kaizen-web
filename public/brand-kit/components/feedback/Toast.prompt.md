**Toast** — brief confirmation or error, tinted by type (success sea-green, warning orange, danger red, info lilac) with a matching leading icon.

```jsx
<Toast type="success" message="Document saved to Records" />
<Toast type="danger" message="Upload failed — try again" />
```
