**Skeleton** — a shimmering placeholder while data loads. Compose several to mirror the final layout.

```jsx
<Skeleton width={180} height={20} />
<Skeleton width="100%" height={64} radius={16} />
```
