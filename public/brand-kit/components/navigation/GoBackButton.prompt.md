**GoBackButton** — a circular back affordance that floats over content (image viewers, detail headers). `variant="glass"` for a translucent dark blur over photography.

```jsx
<GoBackButton onClick={goBack} />
<GoBackButton variant="glass" onClick={goBack} />
```
