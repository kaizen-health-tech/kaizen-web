**TopBar** — screen header with an optional back button, a title (left or centered), and trailing action icons.

```jsx
<TopBar title="Records" onBack={goBack} actions={<IconButton name="Search" />} />
```
