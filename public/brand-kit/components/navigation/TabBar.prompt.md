**TabBar** — bottom navigation for the mobile app: Home, Groups, Kai, Records, with an elevated violet "+" in the middle for creating documents.

```jsx
<TabBar value={tab} onChange={setTab} onAdd={openCreate} />
```
