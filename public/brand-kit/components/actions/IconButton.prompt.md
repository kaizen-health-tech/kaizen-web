**IconButton** — a single icon as a 40×40 tap target. Use for toolbar actions, close/back, overflow menus.

```jsx
<IconButton name="DotsVertical" />
<IconButton name="Close" variant="tonal" />
```
`variant="tonal"` gives a cloud surface fill; default is transparent with a hover surface.
