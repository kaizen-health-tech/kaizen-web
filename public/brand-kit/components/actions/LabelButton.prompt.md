**LabelButton** — a step/segment label for wizards and multi-step forms (onboarding, add-medication). Shows an optional step number and an active/done/idle state.

```jsx
<LabelButton step={1} label="Details" state="active" />
<LabelButton step={2} label="Schedule" state="idle" />
```
