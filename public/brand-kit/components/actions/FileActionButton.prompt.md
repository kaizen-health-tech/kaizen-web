**FileActionButton** — a tinted icon tile for actions on a document (download, share, delete, AI, edit, tag). Use in a row under a file preview.

```jsx
<FileActionButton purpose="ai" label="Summarize" onClick={summarize} />
<FileActionButton purpose="delete" label="Delete" onClick={remove} />
```
