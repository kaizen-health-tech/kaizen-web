**DocumentCard** — a record tile keyed by type (file / medication / note / event), each with its own icon and category tint.

```jsx
<DocumentCard type="medication" title="Lisinopril 10mg" meta="Active · morning" tag="Med" onClick={open} />
```
