**Badge** — tiny uppercase status or count pill in a soft semantic tint. `dot` renders a bare status dot.

```jsx
<Badge tone="success">Active</Badge>
<Badge tone="notice">3 new</Badge>
<Badge tone="error" dot />
```
