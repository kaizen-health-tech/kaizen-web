**ProgressBar** — thin 8px track for uploads and completion. `mode="ai"` wraps it in the violet→pink gradient frame used for Kai's document processing.

```jsx
<ProgressBar value={64} />
<ProgressBar value={40} mode="ai" />
```
