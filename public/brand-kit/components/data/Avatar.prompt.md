**Avatar** — a family member or user. Shows the photo, or violet initials on a light-lilac disc when no image. `ring` adds the gradient story ring.

```jsx
<Avatar name="Maya Chen" src={photo} size={48} ring />
<Avatar name="Dad" />
```
