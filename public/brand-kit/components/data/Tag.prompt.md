**Tag** — labels a relationship or role (e.g. a family member's tie to a record). Heather fill by default; `mode="outlined"` for a ring.

```jsx
<Tag label="Mother" leftIcon="Users" />
<Tag label="Shared" mode="outlined" color="var(--violet)" />
```
