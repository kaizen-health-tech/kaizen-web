**Input** — labelled single-line text field on a soft cloud fill. Violet outline on focus, red on error.

```jsx
<Input label="Full name" placeholder="Jordan Vega" required />
<Input label="Search" leftIcon="Search" rightIcon="Filter" />
<Input label="Weight" prefix="lb" errorText="Enter a number" />
```
