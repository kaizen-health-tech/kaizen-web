**Checkbox** — 8px-rounded square. When checked it fills violet with an aquamarine tick and border (the Kaizen signature pairing).

```jsx
<Checkbox checked={agree} onChange={setAgree} label="I agree to share with my care group" />
```
