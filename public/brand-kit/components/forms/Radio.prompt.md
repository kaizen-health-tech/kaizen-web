**Radio** — one-of-many selector. Selected shows a violet ring with a violet centre dot. Group several under one `name`.

```jsx
<Radio name="plan" checked={plan==='monthly'} onChange={() => setPlan('monthly')} label="Monthly" />
```
