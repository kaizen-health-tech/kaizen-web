**SegmentedControl** — 2–3 mutually exclusive views in a pill. A violet indicator slides under the active segment; the active label turns aquamarine.

```jsx
<SegmentedControl options={["Day","Week","Month"]} value={range} onChange={setRange} />
```
