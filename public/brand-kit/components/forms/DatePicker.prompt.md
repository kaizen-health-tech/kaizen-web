**DatePicker** — a month calendar for scheduling appointments, medication start dates, and events. Selected day is a violet disc; today is ringed.

```jsx
<DatePicker value={date} onChange={setDate} />
```
