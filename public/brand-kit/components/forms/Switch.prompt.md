**Switch** — binary setting toggle. Violet track when on, cloud when off, white thumb. Pass `label` for a settings-row layout.

```jsx
<Switch value={dark} onChange={setDark} label="Dark appearance" />
```
