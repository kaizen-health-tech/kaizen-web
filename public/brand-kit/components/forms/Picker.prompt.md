**Picker** — a scrollable wheel-style option list for frequency, dosage, and unit selection. The selected value is centered and violet.

```jsx
<Picker options={["Once daily","Twice daily","Every 8 hours"]} value={freq} onChange={setFreq} />
```
