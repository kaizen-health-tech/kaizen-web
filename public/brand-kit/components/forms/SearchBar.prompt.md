**SearchBar** — a filled Input preset with a leading search glyph (swaps to a clear × once typing) and a trailing filter action.

```jsx
<SearchBar placeholder="Search records" onSearch={setQuery} onFilter={openFilters} />
```
